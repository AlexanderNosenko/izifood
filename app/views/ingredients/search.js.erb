$(".search-results").html("<%= escape_javascript(render @ingredients ) %>");
if($(".search-results").children().length == 0) 
	$(".search-results").css('display', 'none');
else
	$(".search-results").css('display', 'block');
