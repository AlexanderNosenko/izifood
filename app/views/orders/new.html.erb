<h1>Orders Page</h1>
<div class="container">
  <%= form_for @order do |form| %>
    <div class="ingredients">
	  <div class="recipe-block">
	    <div class="recipe-ingredients">
	      <% @order.items.each do |item| %>
		    <%= render item %>		
	      <% end %>
	    </div>	  
	  </div>
	  <%# @recipes.each do |recipe| %>
  	  	<%# render partial: 'recipes/recipe', locals: {:recipe => recipe, form: form}%>  	  
      <%# end %>
    </div>
    <div class="dashed-border"></div>
    <div class="ingredients">
  	  <div class="recipe-block">
	   <div class="custom-ingredients"></div>
  	  </div>
    </div>
    <%= form.submit 'submit' %>
  <% end %>
  <div class="live-search">
  	<%= render 'live_search' %>
  </div>
</div>