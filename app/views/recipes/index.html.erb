<div class="container">
  <%= render partial: 'filters', locals: {
      categories: categories,
      filters: filters
    } %>
  <div class="recipes">
    <%= render recipes %>
    <%= will_paginate recipes, inner_window: 1, outer_window: 0 %>
  </div>
</div>